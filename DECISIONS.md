# القرارات التقنية وأسبابها (DECISIONS)

## 1. npm workspaces بدل pnpm
pnpm غير مثبّت على الجهاز، وnpm 11 (مضمّن مع Node v24 المثبت) يدعم Workspaces بشكل كافٍ للمشروع
دون خطوة تثبيت إضافية. لا حاجة تقنية قوية لـpnpm هنا.

## 2. SQLite افتراضيًا، PostgreSQL للإنتاج عبر تبديل provider
يسمح بتطوير واختبار فوري بدون تثبيت خادم قاعدة بيانات، مع بقاء المخطط (schema.prisma) قابلًا
للتبديل لـPostgreSQL بتغيير سطر واحد + `DATABASE_URL` (راجع DEPLOYMENT.md). القيد المعروف:
قيود UNIQUE المركّبة التي تحوي حقولًا NULL تتصرف بشكل مختلف قليلًا بين SQLite وPostgreSQL
(SQLite يسمح بعدة NULL في نفس القيد المركّب) — يؤثر فقط على `InsightSnapshot` عند تعذّر ربط
campaign/adset/ad، وهي حالة نادرة موثّقة في الكود.

## 3. JWT بدل الجلسات المخزّنة في قاعدة البيانات
لوحة تحكم بسيطة لمستخدم Admin واحد أو قلة؛ JWT يبسّط التحقق بدون جدول جلسات إضافي. المدة 12
ساعة توازن بين الأمان وسهولة الاستخدام اليومي.

## 4. Bearer Token في localStorage بدل الاعتماد فقط على Cookie
الخادم (منفذ 4000) ولوحة التحكم (منفذ 3000) على أصلين مختلفين محليًا؛ الاعتماد على Cookie
عبر الأصول (Cross-Origin) يعقّد إعداد `SameSite`/`Secure` في التطوير المحلي بدون HTTPS. الخادم
يدعم Cookie **و**Authorization header معًا (`requireAdmin` يتحقق من الاثنين) ليبقى مرنًا لخيار
نشر لاحق على نفس النطاق.

## 5. معالجة Webhook بشكل غير متزامن بعد إرجاع 200 فورًا
Meta تعيد إرسال الحدث إذا لم تستلم 200 خلال مهلة قصيرة. إرجاع 200 فورًا ثم المعالجة (جلب تفاصيل
العميل من Graph API، إلخ) في الخلفية يمنع إعادة الإرسال غير الضرورية، مع الاعتماد على
`WebhookEvent.status` لتتبّع نجاح/فشل المعالجة الفعلية بشكل منفصل عن استلام الحدث.

## 6. Idempotency عبر hash(leadgen_id) وليس معرّف الحدث الخام
Meta لا تضمن دائمًا معرّف حدث فريدًا مستقرًا عبر إعادة الإرسال، لكن `leadgen_id` ثابت لكل عميل
محتمل. استخدام `sha256("leadgen:" + leadgen_id)` كمفتاح فريد يمنع التكرار بشكل موثوق حتى لو
تغيّرت بنية الـpayload الخارجية.

## 7. Conversions API for CRM كطريقة الإرسال الوحيدة الرسمية لحالة "تم تقديم الطلب"
تم التحقق (بحث في توثيق Meta الحالي) أن لا يوجد API عام لقراءة حالة Leads Center، بينما
Conversion Leads Integration هي الطريقة الرسمية المتاحة حاليًا لإرسال إشارات جودة العميل مرة
أخرى إلى Meta. النظام يعاملها كـ"إرسال أفضل جهد" (best-effort) غير حرج — فشلها لا يوقف تحديث
الحالة محليًا، فقط يُسجَّل السبب.

## 8. توحيد أرقام الهاتف العراقية يدويًا فوق libphonenumber-js
`libphonenumber-js` وحده لا يتعامل بمرونة كافية مع الصيغ المحلية الشائعة في العراق (`07XXXXXXXXX`
بدون كود دولة، أو بدون الصفر البادئ). أضفنا طبقة توحيد يدوية قبل التمرير للمكتبة لرفع دقة اكتشاف
التكرار.

## 9. اكتشاف التكرار بالعلم (Flag) وليس الحذف
حذف عميل مكرر تلقائيًا قد يفقد بيانات فعلية (رقم هاتف مختلف قليلًا لعميل آخر بالخطأ). النظام
يعلّم `isDuplicate=true` مع سبب واضح، ويستثنيه من العدّ في التقارير افتراضيًا، لكن يبقى قابلًا
للمراجعة اليدوية وإلغاء العلامة.

## 10. Next.js 14.2.35 (وليس 14.2.15 أو 15/16)
14.2.15 (الإصدار المخطط أوليًا) يحمل ثغرة أمنية معروفة (نُبّه له تلقائيًا عبر npm). تم الترقية
لآخر إصدار مُرقَّع ضمن نفس خط 14.x (`14.2.35`) لتفادي كسر التوافق مع App Router المستخدم، بدل
القفز لإصدار رئيسي أحدث (15/16) الذي يتطلب تغييرات إضافية غير ضرورية للنطاق الحالي.

## 11. عدم ترقية Vitest 2.x → 4.x
`npm audit` يُبلغ عن ثغرة حرجة (critical) في Vitest متعلقة بخادم `--ui` الاختياري الذي لا
يُستخدم في هذا المشروع (تشغيل الاختبارات فقط عبر `vitest run`). الترقية لـVitest 4 تغيير رئيسي
قد يكسر إعداد الاختبارات الحالي؛ تم توثيقها كدين تقني بدل ترقية متسرعة تحت ضغط الوقت.

## 12. رمزان منفصلان فعليًا خلف متغيّر بيئة واحد (Page Token مقابل System User Token)
عند إعداد "نضارة الأفضل لكِ" فعليًا، تبيّن أن الصفحة تستخدم "New Pages Experience"، وأن:
- استدعاء `POST /{page-id}/subscribed_apps` (اشتراك الصفحة بأحداث Webhook) يتطلب حصريًا Page
  Access Token حقيقيًا يُشتق عبر `GET /{page-id}?fields=access_token` باستخدام توكن مستخدم
  النظام (System User) العام.
- بينما استدعاءات Marketing API (بيانات الحملات/الإنفاق عبر `act_{ad_account_id}`) تفشل بنفس
  ذلك الرمز المُشتق (خطأ `#100 Unsupported get request`) لأنه Page-scoped، وتنجح فقط مع رمز
  مستخدم النظام العام نفسه (غير المُشتق).
تم استخدام الرمز المُشتق مرة واحدة فقط لإتمام اشتراك Webhook (إجراء لمرة واحدة، دائم بعدها)،
ثم استعادة رمز مستخدم النظام العام كقيمة دائمة لـ`META_PAGE_ACCESS_TOKEN` لأنه يغطي احتياجات
النظام الفعلية (leads_retrieval + ads_read) في التشغيل اليومي.

## 13. حساب الإعلانات ومستخدم النظام أصلان منفصلان يجب ربطهما كليهما
ربط تطبيق "جرد كلاود" بحساب الإعلانات "نضارة" من صفحة "التطبيقات" في Business Settings لا يمنح
مستخدم النظام (System User) نفسه صلاحية الوصول لذلك الحساب تلقائيًا؛ يجب تعيين حساب الإعلانات
صراحة كأصل لمستخدم النظام أيضًا من صفحة "الأصول المعينة" الخاصة به (تمامًا كما تم مع الصفحة).

## 14. اكتشاف مهم: `onsite_conversion.messaging_order_created_v2` كمقياس طلبات رسمي لكل إعلان
عند محاولة ربط 77 طلبًا حقيقيًا (من محادثات Instagram DM) بالفيديو المصدر، فشلت كل الطرق
التقليدية (CSV Leads Center بلا Lead ID/هاتف، فتح المحادثات فرديًا عبر أتمتة متصفح غير موثوقة).
الحل الفعلي: حقل `actions` في Marketing API Insights (مستوى `ad`) يحتوي إجراء
`onsite_conversion.messaging_order_created_v2` — وهو حدث "طلب تم إنشاؤه" الذي تتتبعه Meta
تلقائيًا لكل إعلان يقود محادثات مراسلة. تم التحقق: مجموع هذا الإجراء عبر كل الإعلانات يطابق
تمامًا رقم "الطلبات" على مستوى الحساب الكامل (48=48). هذا يوفر تكلفة الطلب الحقيقية لكل
إعلان/فيديو **بدون الحاجة لأي مطابقة يدوية أو أتمتة متصفح**، وهو أدق وأوثق من محاولة مطابقة
كل عميل على حدة. يُنصح باعتماد هذا الإجراء كمصدر أساسي لحساب "الطلبات" في تقارير الإعلانات
لهذا النوع من الأعمال (مبيعات عبر محادثات مباشرة)، بدل الاعتماد فقط على Leads المستقبلة عبر
Webhook (وهي غير موجودة أصلًا لهذا النوع من الحملات).

## 16. لم يُبنَ خط أنابيب تحليل محادثات (Claude + Webhook رسائل) رغم طلب صريح
طُلب مني بناء نظام كامل يستقبل رسائل Messenger/Instagram، يحلّلها بـAnthropic API لاكتشاف
الحجوزات، ويستخرج الاسم/الهاتف/العنوان من نص المحادثة. تحققت فعليًا عبر
`GET /me/permissions` أن الرمز الحالي يملك فقط: `pages_show_list, ads_read, leads_retrieval,
pages_read_engagement, pages_manage_metadata, public_profile` — **لا توجد** `pages_messaging`
ولا `instagram_manage_messages` إطلاقًا. بدون هاتين الصلاحيتين، Meta لن ترسل أي محتوى رسائل
لأي Webhook مهما كان الكود صحيحًا. الحصول عليهما يتطلب Meta App Review منفصلًا وأكثر
تشددًا (صلاحيات مراسلة حساسة)، وليس تعديلًا برمجيًا. بناء الخدمة دون هذه الصلاحية كان سينتج
كودًا "يعمل" لكن لا يستقبل بيانات حقيقية أبدًا — تحديدًا الحل الوهمي الذي طلب المستخدم نفسه
تجنّبه صراحةً. تم بدلًا من ذلك بناء نظام Delta حقيقي بالكامل (انظر #17) يحقق نفس الهدف
(جرد آخر 24 ساعة + تكلفة الطلب لكل فيديو) من مصدر بيانات نملك صلاحيته فعليًا.

## 17. تتبّع الفروقات (Delta) بدل القيمة التراكمية الخام لـmessaging_order_created_v2
قيمة `onsite_conversion.messaging_order_created_v2` التي تعيدها Meta لليوم الجاري تراكمية طوال
ذلك اليوم (ترتفع كل ساعة). لحساب "كم طلبًا جديدًا خلال آخر 24 ساعة" لا يمكن أخذ القيمة كما هي؛
يجب مقارنتها بآخر قيمة محفوظة لنفس (الإعلان + اليوم). أُضيف لهذا:
- `AdPerformanceSnapshot`: سجل تاريخي لكل مزامنة (وليس Upsert يكتب فوق القديم كـ`InsightSnapshot`).
- `DetectedOrderIncrement`: الفرق المحسوب (`max(0, current - previous)`) مع مفتاح فريد
  `(adId, snapshotId)` يمنع احتساب نفس اللقطة مرتين.
- أول لقطة لأي (إعلان+يوم) تُعامَل كـ`Baseline` (لا Increment لها) لتفادي احتساب كل التاريخ
  كـ"طلبات جديدة" عند أول تشغيل.
- قفل مزامنة (`syncInProgress`) في نفس العملية يمنع تشغيل مزامنتين معًا.
تم التحقق مباشرة مقابل بيانات Meta الحقيقية: مزامنتان متتاليتان بفارق ~10 دقائق أنتجتا
Increment واحدًا صحيحًا (+1 طلب حقيقي حدث بينهما) بينما مزامنة ثالثة فورية أنتجت صفرًا -
يطابق تمامًا السلوك المطلوب (لا تكرار).

## 15. مصدر الفيديو: محاولات متعددة بالترتيب + تسجيل السبب عند الفشل
بما أن بنية `creative` تختلف جذريًا بين إعلان فيديو كلاسيكي وReel وDynamic Creative ومنشور
موجود، تم تصميم `fetchAndExtractCreative` كسلسلة محاولات مرتّبة بدل افتراض بنية واحدة، مع حقل
`extractionNote` صريح بدل ترك القيمة `null` بلا تفسير (يلبي متطلب "لا تترك خطأ صامتًا").

## 18. الانتقال من SQLite إلى PostgreSQL عند بناء Multi-Tenant
قاعدة Multi-tenant تحتاج قيود UNIQUE مركّبة (`[workspaceId, metaXxxId]`) على كل جدول بيانات Meta،
وحقول تشفير/JSON أكثر، وتحتمل حجم بيانات أكبر بمرور الوقت مع تعدد المستأجرين. تم تغيير
`schema.prisma` بالكامل إلى `provider = "postgresql"` بدل الإبقاء على SQLite للتطوير. الجهاز لا
يملك Docker مثبّتًا وقت الكتابة (كما في PROGRESS.md)، لذا التشغيل المحلي يتطلب إما تثبيت Docker
لتشغيل خدمة `postgres` الموجودة أصلًا في `docker-compose.yml`، أو الاتصال بقاعدة Postgres سحابية
مجانية مؤقتة (Neon/Supabase) عبر `DATABASE_URL` — راجع "الخطوات المتبقية" في PROGRESS.md.

## 19. نقل بيانات SQLite القديمة عبر Prisma Client ثانٍ بدل better-sqlite3
حاولت أولًا استخدام `better-sqlite3` لقراءة قاعدة SQLite القديمة مباشرة في سكربت الهجرة، لكنه
حزمة native تتطلب `node-gyp` + Python للبناء، وفشل التثبيت فعليًا على هذا الجهاز (لا توجد نسخة
Python مدعومة). البديل: نسخة مطابقة من المخطط القديم محفوظة في
`server/prisma/legacySqlite/schema.prisma` (نفس الجداول القديمة تمامًا، provider=sqlite) تُولِّد
Prisma Client منفصلًا (`node_modules/.legacy-sqlite-client`، لا يتطلب أي بناء native لأن محرك
Prisma مُجمَّع مسبقًا). سكربت `scripts/migrate-sqlite-to-postgres.ts` يستخدم هذا العميل للقراءة
فقط، والعميل الرئيسي (Postgres) للكتابة، ضمن معاملة واحدة، محافظًا على نفس قيم `id` الأصلية حتى
تبقى كل العلاقات (campaignId/adSetId/...) صحيحة بعد النقل.

## 20. استمرار خط Webhook/Insights/Sheets القديم عبر `LEGACY_WORKSPACE_ID` بدل توجيه Multi-Tenant كامل
لم يُبنَ توجيه Webhook متعدد المستأجرين في هذه المرحلة (كل صفحة تحتاج اشتراك Webhook مستقل + ربط
Meta App منفصل لكل Workspace - تعقيد كامل مؤجَّل لمرحلة لاحقة). بدل ذلك، أُضيف متغيّر بيئة واحد
`LEGACY_WORKSPACE_ID` يحدّد أي Workspace يستمر خط Webhook/Insights/Google Sheets الحالي (الخاص
بعملك الفعلي "نضارة") بالكتابة إليه، دون أي تغيير في سلوكه الوظيفي - فقط إضافة `workspaceId` لكل
عملية إنشاء/بحث في `attribution.ts`, `routes/webhook.ts`, `jobs/insights.ts`, `services/sheets.ts`
لأن الجداول أصبحت UNIQUE بشكل مركّب (`[workspaceId, metaXxxId]`) بدل حقل مفرد.

## 21. Super Admin كحقيقة منفصلة تمامًا عن `User` (وليس دورًا "role" داخل نفس الجدول)
لو كان Super Admin مجرد صف بحقل `role="superadmin"` داخل جدول `User` العادي، فإن أي ثغرة في منطق
تفويض الأدوار (أو JWT مسروق لمستخدم عادي) قد تُستغل نظريًا للوصول لصلاحيات إيقاف/حظر أي عميل. تم
عزله في جدول ونظام مصادقة JWT منفصلين تمامًا (`SUPERADMIN_JWT_SECRET` مختلف عن `JWT_SECRET`،
كوكي مختلف)، مع فرض 2FA (TOTP عبر `otplib`) إلزاميًا على كل حساب Super Admin منذ إنشائه
(`seedWorkspace.ts` يولّد السر ويعرض QR Code فورًا) - لا يوجد مسار لإنشاء Super Admin بدون 2FA.
